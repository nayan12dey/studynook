export const metadata = {
  title: "StudyNook - Login",
  description: "Sign in to your StudyNook account to manage your bookings.",
};

export default function LoginLayout({ children }) {
  return children;
}
