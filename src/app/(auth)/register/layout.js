export const metadata = {
  title: "StudyNook - Register",
  description: "Create a free StudyNook account and start booking study rooms today.",
};

export default function RegisterLayout({ children }) {
  return children;
}
